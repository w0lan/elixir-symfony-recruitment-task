### Projekt
Zadanie rekrutacyjne: **Phoenix (Elixir) + Symfony (PHP)**, komunikacja po **REST API (JSON)**.

### Proces pracy (dla osoby sprawdzającej)
- Każdy task realizujemy w osobnym branchu w formacie: `task/<nr>-<slug>`.
- Tagi stosujemy tylko dla kamieni milowych (np. `milestone/submission`).

### Taski
Taski są opisane w `docs/tasks/`.
